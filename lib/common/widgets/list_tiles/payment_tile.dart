import 'package:flutter/material.dart';

class KPaymentTile extends StatelessWidget {
  const KPaymentTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile();
  }
}
